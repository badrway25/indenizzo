Django>=5.2,<5.3
django-environ>=0.11
django-extensions>=3.2
django-htmx>=1.21
djangorestframework>=3.15
django-filter>=24.3
django-modeltranslation>=0.19
django-import-export>=4.1
django-simple-history>=3.7
django-auditlog>=3.0
celery>=5.4
redis>=5.0
psycopg[binary]>=3.2
Pillow>=10.4
reportlab>=4.2
python-slugify>=8.0
requests>=2.32
beautifulsoup4>=4.12
lxml>=5.2
openpyxl>=3.1
pandas>=2.2
pytest>=8.3
pytest-django>=4.9
factory-boy>=3.3
ruff>=0.6
black>=24.8
mypy>=1.11
coverage>=7.6
playwright>=1.47
# Sentry — error monitoring opzionale. Inizializzato solo se SENTRY_DSN
# è configurato (vedi apps/core/observability.py). Pinning sulla 2.x
# per evitare break-changes della 3.x ancora non rilasciata stabile.
sentry-sdk[django]>=2.18,<3
# MFA admin — django-otp + plugin TOTP. NON aggiunti a
# INSTALLED_APPS di default per evitare migrazioni schema premature
# su ambienti dev. Vedi
# docs/architecture/LOCAL_PRODUCT_HARDENING_PASS5_MFA_ADMIN.md per
# il flusso di attivazione (locale-first → staging → prod).
django-otp>=1.5,<2
qrcode>=7.4,<9
# Content-Security-Policy header emission (F-p0-codice-4-csp).
# Pin alla 4.x: API stabile, supporta nonce e direttive
# tradizionali via settings. Non a INSTALLED_APPS — solo middleware.
django-csp>=4.0,<5
# Static-file serving with auto-compression (F-p2-perf-1). Standard
# Django add-on for serving static assets with gzip/brotli. In dev
# (DEBUG=True) we set WHITENOISE_USE_FINDERS=True so it serves
# directly from the finders, without requiring `collectstatic`. In
# prod the CDN typically owns transport compression; WhiteNoise
# remains as a safety net for static-file delivery.
whitenoise>=6.6,<7
# Font subsetting toolchain (F-p2-perf-4). Used by
# `scripts/subset_arabic_fonts.py` to re-subset Amiri 700 against
# the actual Arabic corpus used in the platform (templates + .po),
# bringing the Arabic font ship weight from ~98 KB to ~30 KB.
# The `[woff]` extra pulls in `brotli` so pyftsubset can emit
# woff2 directly. Not loaded at runtime by Django — script-side
# dependency only.
fonttools[woff]>=4.50
